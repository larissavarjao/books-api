const getAllBooks = () => {
  console.log("getAllBooks");
};

const getBookById = (id) => {
  console.log(id);
};

const insertBook = (newBook) => {
  console.log(newBook);
};

module.exports = {
  getAllBooks,
  getBookById,
  insertBook,
};
